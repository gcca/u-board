services:
  u-board:
    build:
      context: .
    image: u-board:latest
    ports:
      - "5561:5561"
    environment:
      DATABASE_URL: sqlite:/app/data/u-board.db
    volumes:
      - db:/app/data
    restart: unless-stopped

volumes:
  db:
